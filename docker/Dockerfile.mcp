FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir mcp httpx starlette

COPY src/mcp/ src/mcp/

EXPOSE 8001

CMD ["uvicorn", "src.mcp.server.weather_server:starlette_app", "--host", "0.0.0.0", "--port", "8001"]
